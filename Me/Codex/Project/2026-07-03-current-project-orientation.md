# 2026-07-03 Current Project Orientation

- Task: inspect the current `GodotUI` workspace.
- Entry: `Me/Codex` did not exist, so the root directory was checked next.
- Scope checked: root file list and `godot_mvvmc_ui_framework.svg` structure/text.
- Current state: the workspace contains one SVG diagram, empty `.agents` and `.git` directories, and no runnable Godot project files detected.
- Git state: `git rev-parse --show-toplevel` fails, so the local `.git` directory is not a valid repository metadata directory.
- Main content: the SVG describes a Godot MVVM-C UI framework driven by source manifests, editor/CLI generation, runtime `UIManager -> Controller -> Store -> Binder -> Control`, and generated Godot outputs.
